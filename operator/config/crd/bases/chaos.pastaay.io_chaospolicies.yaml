---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: chaospolicies.chaos.pastaay.io
spec:
  group: chaos.pastaay.io
  names:
    kind: ChaosPolicy
    listKind: ChaosPolicyList
    plural: chaospolicies
    singular: chaospolicy
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: ChaosPolicy is the Schema for the chaospolicies API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ChaosPolicySpec defines the desired state of ChaosPolicy
            properties:
              dropConnection:
                description: 'Forcefully drop the connection (requires target: all
                  or database)'
                type: boolean
              duration:
                description: Duration of the chaos experiment (e.g., "30s", "2m").
                  If omitted, the policy runs indefinitely.
                type: string
              errorBody:
                description: Custom error message body to be returned
                type: string
              errorChance:
                description: Probability of injecting a synthetic error (0.0 to 1.0)
                maximum: 1
                minimum: 0
                type: number
              errorCode:
                description: Custom HTTP/gRPC status code (e.g., 500, 404, 13)
                type: integer
              latencyChance:
                description: Probability of injecting latency (0.0 to 1.0)
                maximum: 1
                minimum: 0
                type: number
              latencyDuration:
                description: Delay duration (e.g., "500ms", "2s")
                type: string
              target:
                description: Target endpoint, query, or topic (e.g., "/api/v1/users",
                  "SELECT 1")
                type: string
              type:
                description: 'Protocol type: http, sql, grpc, redis, mongo, kafka,
                  rabbitmq, resource'
                enum:
                - http
                - sql
                - grpc
                - redis
                - mongo
                - kafka
                - rabbitmq
                - resource
                type: string
            required:
            - target
            - type
            type: object
          status:
            description: ChaosPolicyStatus defines the observed state of ChaosPolicy
            properties:
              conditions:
                description: conditions represent the current state of the ChaosPolicy
                  resource
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastAppliedTime:
                description: Timestamp of the last successful synchronization with
                  the engine
                format: date-time
                type: string
              phase:
                description: Represents the overall state of the policy (e.g., Applied,
                  Failed)
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
